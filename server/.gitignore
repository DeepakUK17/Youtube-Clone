# dependencies
node_modules/

# environment variables (NEVER commit these)
.env
.env.*
.env.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# uploads (video files should not be in git)
uploads/
